apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: coder-ui-dev
spec:
  rules:
  - host: coder.api.encodedcc.org
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: coder-dev
            port:
              number: 80
